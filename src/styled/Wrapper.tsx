import styled from '@emotion/styled';

const ContentWrapperMaxWidth = styled.div`
    max-width: 1560px;
    padding: 0 1rem;
`

const ContentBodyWrapper = styled.div`
    min-height: calc(100vh - 80px - 50px);
`

export {
    ContentWrapperMaxWidth,
    ContentBodyWrapper
}