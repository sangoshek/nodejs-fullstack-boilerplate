import styled from '@emotion/styled';
import {
    Stack
} from '@mui/material';

const StyledFooter = styled(Stack)`
    background-color: #efefef;
    color: #000;
    min-height: 50px;
` as typeof Stack

export {
    StyledFooter
}